#include <bits/stdc++.h>

using namespace std;



int main () {

	#ifdef BROKEN
	freopen ("in", "w", stdout);
	#endif

	srand (time (0));

	int n = 10, m = 20, q = 30;

	cout << n << " " << m << " " << q << "\n";

	for (int i = 1; i <= m; ++i) {
		
	}

	return 0;
}