#include <bits/stdc++.h>

using namespace std;

const int N = 1e8;

inline string do_smth (string && s) {
	
}

int main () {
	freopen ("in", "r", stdin);
	freopen ("out", "w", stdout);

	srand (time (0));
	string s;
	s.resize (0);
   	for (int i = 0; i < N; ++i)
   		s += char (rand ());
   	do_smth ();
	return 0;
}