/**
	Oh, my dear
**/

#include <bits/stdc++.h>

using namespace std;


inline bool bit (int mask, int pos) {
	return (mask >> pos) & 1;
}

int main () {

	#ifdef BROKEN
	freopen ("in", "r", stdin);
	#endif

	ios_base :: sync_with_stdio (0);
	cin.tie (0);

	cin >> n;

	d[0][0] = 1;

	for (int i = 1; i <= n; ++i) {
		
	}
	
	
	return 0;
}