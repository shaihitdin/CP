#include <bits/stdc++.h>

using namespace std;

map <int, string> Map2;
map <string, int> Map;

int correct[];

inline bool check_correct (const int &v) {
	for (int to : g_rev[v]) {
	
	}
}

int main () {

	freopen ("strings.in", "r", stdin);
	freopen ("strings.out", "w", stdout);

	cin >> t;

	cin >> k;

	for (int i = 1; i <= k; ++i)
		cin >> a[i];
		
	for (int i = 0; i < t.size (); ++i) {
	
	}
	
	return 0;
}