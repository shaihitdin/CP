#include <bits/stdc++.h>

using namespace std;

int main () {

	#ifdef LOCAL
	freopen ("railroad.in", "r", stdin);
	freopen ("railroad.out", "w", stdout);
	#endif

	ios_base :: sync_with_stdio (0);
	cin.tie (0);

	cin >> n >> m >> x;

	for (int i = 1; i <= n; ++i) {
		int 
	}

	return 0;
}