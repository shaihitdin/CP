#include <bits/stdc++.h>

using namespace std;

struct point {
	int x, y, id, type;
};

vector <point> cc;

inline bool operator < (const point &

int main () {

	#ifdef LOCAL
	freopen ("in", "r", stdin);
	#endif

	ios_base :: sync_with_stdio (0);
	cin.tie (0);

	cin >> n;

	for (int i = 1; i <= n; ++i) {
		int sx, sy, fx, fy;
		cin >> sx >> sy >> fx >> fy;
	}

	return 0;
}