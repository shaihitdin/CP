#include <bits/stdc++.h>

using namespace std;

#define y1 works

int main () {

	freopen ("in", "r", stdin);
	freopen ("out", "w", stdout);

	scanf ("%d%d%d\n", &w, &h, &n);

	int x1, y1, x2, y2;

	x1 = 0, y1 = 0, x2 = w, y2 = h;

	for (int i = 1; i <= n; ++i) {
		scanf ("%c%d\n", &q, &x);
		if (q == 'H') {
			
		}
		else {
		
		}
	}

	return 0;
}