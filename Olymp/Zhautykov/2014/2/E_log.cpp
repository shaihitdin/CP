#include <bits/stdc++.h>

using namespace std;

struct node {
	int x, y, mn;
	node (const int &element) {
		x = mn = element;
		y = rand();
	}
};

int main () {
	freopen ("in", "r", stdin);
	freopen ("out", "w", stdout);
	scanf ("%d%d", &n, &k);

	return 0;
}