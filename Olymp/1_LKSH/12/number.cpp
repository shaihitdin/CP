#include<iostream>
#include<stdio.h>
#include<math.h>
using namespace std;
const int N = 100001;
int d[N], d1[N], d2[N];
int main() {
	freopen("number.in", "r", stdin);
	freopen("number.out", "w", stdout);
	cin >> n >> k;
	for(i = 1; i <= k; ++i)
		d[i] = 1, d2[i] = 1;
	for(i = k + 1; i <= n; ++i) {
		tmp = sqrt(i) + 1;
		for(j = 1; j 
	}
return 0;
}