/**
	Oh, my dear
**/

#include <bits/stdc++.h>

using namespace std;

typedef long long ll;

const int N = 2e5 + 100;

int a[N], n;

inline ll sqr (ll x) {
	return x * x;
}

int main () {
	
	freopen ("cbarn.in", "r", stdin);
	freopen ("cbarn.out", "w", stdout);
	
	ios_base :: sync_with_stdio (0);
	cin.tie (0);

	cin >> n;

	for (int i = 0; i < n; ++i)
		cin >> a[i];
	
	
	return 0;
}