#include <bits/stdc++.h>

using namespace std;

const int N = 20;

vector <int> d[1 << N];

int main () {

	freopen ("in", "r", stdin);
	freopen ("out", "w", stdout);


	ios_base :: sync_with_stdio (0);

	cin >> n >> l;

	for (int i = 1; i <= n; ++i) {
		cin >> t[i] >> cnt[i];
		for (int j = 1; j <= cnt[i]; ++j)
			cin >> a[i][j];
	}

	for (int i = 1; i <= n; ++i) {
	
	}
	
	return 0;
}