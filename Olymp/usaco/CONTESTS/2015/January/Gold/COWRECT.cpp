#include <bits/stdc++.h>

using namespace std;



int main () {

	freopen ("in", "r", stdin);
	freopen ("out", "w", stdout);

	ios_base :: sync_with_stdio (0);

	cin >> n;

	for (int i = 1; i <= n; ++i) {
		int x, y;
		cin >> x >> y >> c;
		++cnt[x][y];
	}

	for (int x = 0; x < N; ++x) {
		
	}
	
	return 0;
}