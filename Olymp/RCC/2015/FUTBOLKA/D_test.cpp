#include <bits/stdc++.h>

using namespace std;


int main () {

	freopen ("in", "r", stdin);
	freopen ("out", "w", stdout);

	int n = 5;
	
	for (int i = 1; i <= n; ++i) {
		memset (dp, 0, sizeof (dp));
		dp[1][0] = 1;
		for (int j = 1; j <= i; ++j) {
			for (int k = 0; k <= j; ++k) {
				if (k) {
					
				} else {
				
				}
			}
		}
	}

	return 0;
}