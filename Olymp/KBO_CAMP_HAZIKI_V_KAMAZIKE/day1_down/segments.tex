\begin{problem}{“очки и отрезки}{stdin}{stdout}{2 секунды}

%јвтор: —ергей ѕоромов

ƒано $n$ отрезков на числовой пр€мой и $m$ точек на этой же пр€мой.
ƒл€ каждой из данных точек определите, скольким отрезкам она принадлежит.
“очка $x$ считаетс€ принадлежащей отрезку с концами $a$ и $b$, если 
выполн€етс€ двойное неравенство $min(a, b) \le x \le max(a, b)$.

\InputFile

ѕерва€ строка содержит два целых числа $n$~($1 \le n \le 10^5$)~--- число отрезков и $m$~($1 \le m \le 10^5$)~--- число точек.
¬ следующих $n$ строках по два целых числа $a_i$ и $b_i$~--- координаты концов соответствующего отрезка.
¬ последней строке $m$ целых чисел~--- координаты точек.
¬се числа во входном файле не превосход€т по модулю $10^9$.

\OutputFile

¬ выходной файл выведите $m$ чисел~--- дл€ каждой точки количество отрезков, в которых она содержитс€.

\Examples

\begin{example}%
\exmp{
2 2
0 5
7 10
1 6
}{
1 0
}%
\exmp{
1 3
-10 10
-100 100 0
}{
0 0 1
}%
\end{example}


\end{problem}
             