#include<iostream>
#include<stdio.h>
#include<string>
using namespace std;
int n,i;
string s;
string 
int main(){
	ios_base::sync_with_stdio(0);
	freopen("in","r",stdin);
	freopen("out","w",stdout);
	cin>>n;
	for(i=1;i<=n;++i){
		cin>>s;
		memset(d,-1,sizeof(d));
		for(j=0;j<s.size();++j){
			if('0'<=s[j] && s[j]<='9'){
				if(d[j-1]!=-1){
					d[j]=
				}
			}
		}
	}
return 0;
}