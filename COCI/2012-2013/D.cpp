#include <iostream>
#include <stdio.h>
using namespace std;
struct meal {
	int a, b, num;
	bool operator 
}
int main () {
	freopen ("in", "r", stdin);
	freopen ("out", "w", stdout);
	scanf ("%d", &n);
	for (int i = 1; i <= n; ++i) {
		scanf ("%d %d", &a[i], &b[i]);
	}
	return 0;
}